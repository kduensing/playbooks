---
- name: update Unbuntu repository and distros
  hosts: ubuntu
  become: yes
  tasks:
    - name: update package repository
      apt: 
        update_cache: yes
      
    - name: update all packages
      apt: 
        upgrade=dist